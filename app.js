// --- MASSIVE DICTIONARY (1500+ Words) ---
// Contains standard, clinical, anatomical, botanical, nostalgic, and hostile vocabulary.
const wordBankString = "yes,no,here,there,help,stop,run,hide,dark,light,red,blue,pink,white,wire,web,circuit,medical,clinic,specimen,circle,square,kitten,cat,brother,sleep,wake,biphasic,night,morning,plant,botany,monstera,rhaphidophora,tetrasperma,soil,water,root,stem,leaf,node,propagate,cut,aerial,alien,nine,lain,chobits,persocom,celeste,game,play,terminal,code,scan,ingredient,food,skin,mammal,blood,bone,heart,lung,breath,cold,hot,warm,freeze,burn,fire,ash,dust,ghost,spirit,entity,energy,frequency,static,noise,loud,quiet,whisper,scream,talk,listen,door,window,wall,floor,ceiling,house,room,home,mobile,yard,clean,dirt,wash,glass,mirror,reflect,shadow,shape,figure,man,woman,child,boy,girl,tall,short,fast,slow,now,later,soon,never,always,time,clock,hour,minute,second,day,week,month,year,past,future,present,memory,forget,remember,mind,brain,thought,idea,dream,nightmare,awake,tired,exhausted,rest,bed,pillow,blanket,chair,table,desk,computer,screen,keyboard,mouse,phone,device,power,battery,charge,plug,cable,cord,connection,lost,found,seek,search,find,hide,reveal,secret,truth,lie,false,true,real,fake,illusion,vision,sight,blind,eye,ear,deaf,mouth,mute,speak,tongue,tooth,bite,chew,swallow,drink,eat,hungry,thirsty,full,empty,heavy,light,weight,float,sink,drown,swim,fly,fall,drop,break,fix,repair,shatter,crack,split,join,bind,tie,knot,rope,string,thread,needle,sew,stitch,cut,knife,blade,sharp,dull,blunt,hit,strike,punch,kick,push,pull,drag,carry,lift,hold,catch,throw,toss,spin,turn,twist,bend,fold,crease,flat,smooth,rough,bump,hole,gap,space,void,abyss,deep,shallow,surface,edge,center,middle,side,left,right,up,down,north,south,east,west,direction,path,road,street,trail,track,walk,sprint,jog,crawl,creep,slide,slip,trip,stand,sit,wait,go,start,finish,end,begin,first,last,next,previous,before,after,early,late,when,where,why,how,who,what,which,this,that,these,those,it,they,them,we,us,you,me,I,my,mine,yours,theirs,ours,some,all,none,many,few,much,less,more,most,least,big,small,large,tiny,huge,giant,micro,macro,scale,size,measure,length,width,height,depth,volume,mass,density,solid,liquid,gas,plasma,state,change,shift,move,still,calm,storm,wind,rain,snow,ice,cloud,sky,sun,moon,star,planet,universe,world,earth,ground,rock,stone,sand,metal,iron,steel,gold,silver,copper,wood,tree,forest,jungle,desert,ocean,sea,lake,river,stream,element,nature,city,town,village,building,apartment,hall,gate,fence,roof,brick,paint,color,shade,tint,hue,bright,dim,glow,shine,flash,spark,flame,smoke,melt,chill,shiver,shake,quake,tremble,vibrate,pulse,beat,rhythm,sound,note,music,song,sing,voice,echo,silence,hush,look,see,watch,stare,glare,blink,wink,tear,cry,weep,sob,laugh,smile,grin,frown,angry,sad,happy,joy,fear,scared,terror,horror,panic,dread,peace,anger,rage,fury,mad,crazy,insane,sane,think,know,learn,study,read,write,book,page,word,letter,number,math,count,add,subtract,multiply,divide,equal,sum,total,part,whole,piece,slice,smash,crash,destroy,build,make,create,form,design,art,draw,picture,image,photo,camera,lens,focus,blur,clear,protocol,proxy,server,host,domain,network,signal,wave,transmit,receive,broadcast,channel,station,radio,antenna,radar,sonar,scope,monitor,display,pixel,byte,data,file,folder,directory,system,error,fault,glitch,bug,virus,infection,symptom,cure,medicine,pill,drug,dose,inject,syringe,vein,artery,muscle,flesh,tissue,cell,gene,dna,born,die,death,life,live,survive,perish,decay,rot,spoil,ruin,waste,trash,garbage,scrap,dustbin,ash,cinder,ember,coal,fuel,gasoline,oil,grease,slick,slip,glide,friction,rub,scrape,scratch,itch,tickle,pinch,squeeze,press,crush,grind,mill,powder,dust,grain,seed,spore,pollen,flower,bloom,petal,thorn,bush,shrub,vine,weed,grass,lawn,meadow,field,farm,crop,harvest,gather,collect,hoard,save,spend,buy,sell,trade,barter,value,price,cost,cheap,dear,rich,poor,wealth,money,coin,bill,note,bank,store,shop,market,goods,wares,item,thing,object,tool,weapon,gun,bow,arrow,shield,armor,helmet,suit,clothes,shirt,pants,shoe,boot,glove,hat,coat,dress,skirt,fabric,cloth,silk,cotton,wool,leather,skin,fur,hair,feather,scale,shell,bone,horn,claw,fang,tooth,beak,wing,tail,fin,web,nest,hive,den,cave,burrow,hole,pit,trench,ditch,canal,pipe,tube,hose,pump,valve,seal,gasket,joint,hinge,lock,key,door,knob,handle,lever,switch,button,dial,gauge,meter,scale,ruler,compass,map,chart,graph,line,point,dot,curve,angle,corner,edge,border,limit,bound,range,scope,field,area,zone,region,sector,district,county,state,nation,country,border,guard,watch,patrol,police,law,rule,order,chaos,mess,tidy,neat,clean,dirty,filthy,pure,foul,sweet,sour,bitter,salt,spicy,bland,taste,smell,odor,scent,perfume,stink,reek,noise,sound,quiet,silent,mute,deaf,listen,hear,speak,talk,say,tell,ask,answer,question,reply,echo,voice,tone,pitch,loud,soft,high,low,fast,slow,quick,rapid,swift,brisk,lazy,idle,busy,work,play,rest,sleep,wake,dream,nightmare,vision,hallucination,mirage,ghost,phantom,specter,wraith,demon,angel,god,devil,monster,beast,creature,alien,stranger,friend,enemy,foe,ally,partner,mate,couple,pair,twin,clone,copy,fake,real,true,false,lie,truth,fact,fiction,story,tale,myth,legend,history,past,present,future,time,space,void,empty,full,all,nothing,something,anything,everything,zero,one,two,three,four,five,six,seven,eight,nine,ten,hundred,thousand,million,billion,infinite,endless,forever,never,always,often,rare,seldom,sometimes,maybe,perhaps,sure,certain,doubt,guess,know,think,believe,trust,faith,hope,fear,dread,panic,terror,horror,scare,fright,shock,surprise,amaze,wonder,awe,marvel,miracle,magic,spell,charm,curse,hex,jinx,luck,fate,destiny,doom,glory,honor,shame,guilt,sin,crime,law,judge,jury,trial,prison,jail,cell,bar,chain,rope,tie,bind,free,loose,escape,flee,run,hide,seek,find,lose,win,fail,beat,strike,hit,punch,kick,slap,bite,scratch,cut,tear,rip,break,smash,crush,grind,chew,swallow,spit,vomit,choke,cough,sneeze,breathe,gasp,sigh,yawn,stretch,bend,flex,relax,tense,tight,loose,stiff,limp,soft,hard,firm,solid,liquid,gas,melt,freeze,boil,burn,scorch,char,ash,smoke,fire,flame,spark,light,dark,shadow,shade,color,hue,tint,paint,draw,sketch,trace,copy,print,type,write,read,book,page,word,letter,number,sign,symbol,mark,stamp,seal,stamp,post,mail,send,receive,give,take,keep,hold,drop,throw,catch,fetch,bring,carry,lift,push,pull,drag,slide,roll,spin,turn,twist,bend,fold,crease,wrinkle,smooth,flat,level,even,odd,strange,weird,bizarre,creepy,eerie,spooky,scary,haunted,cursed,blessed,holy,sacred,divine,pure,clean,dirty,filthy,messy,tidy,neat,order,chaos,random,chance,luck,fate,destiny,doom,gloom,sad,happy,mad,glad,joy,sorrow,grief,pain,hurt,ache,sore,wound,scar,blood,bleed,heal,cure,medicine,drug,pill,dose,shot,needle,syringe,doctor,nurse,clinic,hospital,ward,bed,sheet,blanket,pillow,sleep,dream,wake,arise,stand,sit,lie,walk,run,jump,leap,fall,drop,sink,float,swim,fly,soar,glide,dive,plunge,crash,smash,break,fix,mend,repair,build,make,create,destroy,ruin,spoil,rot,decay,fade,wither,bloom,grow,shrink,swell,burst,pop,snap,crack,split,divide,share,part,piece,whole,all,none,some,few,many,much,more,less,least,most,best,worst,good,bad,evil,wicked,kind,cruel,harsh,soft,gentle,rough,tough,hard,easy,simple,complex,hard,difficult,solve,answer,question,ask,tell,say,speak,listen,hear,deaf,mute,blind,see,look,watch,stare,glare,blink,wink,eye,tear,cry,weep,sob,laugh,smile,grin,frown,pout,kiss,hug,hold,touch,feel,sense,nerve,brain,mind,soul,spirit,ghost,phantom,shadow,dark,light,sun,moon,star,sky,cloud,rain,snow,ice,wind,storm,thunder,lightning,fire,water,earth,air,metal,wood,stone,rock,sand,dust,ash,dirt,mud,clay,soil,root,stem,leaf,flower,seed,fruit,tree,bush,grass,weed,plant,animal,beast,bird,fish,bug,worm,snake,lizard,frog,toad,mouse,rat,cat,dog,wolf,bear,lion,tiger,horse,cow,pig,sheep,goat,chicken,duck,goose,swan,eagle,hawk,owl,crow,raven,dove,pigeon,sparrow,robin,bluebird,cardinal,finch,woodpecker,parrot,ostrich,penguin,seal,whale,dolphin,shark,crab,lobster,shrimp,clam,oyster,snail,slug,spider,ant,bee,wasp,fly,mosquito,moth,butterfly,beetle,cricket,grasshopper,flea,tick,louse,mite";
const words = wordBankString.split(",");

const sentences = [
    "I am here.", "Behind you.", "Too loud.", "Look closely.", 
    "It is cold.", "Can you see?", "Do not leave.", "We are waiting.",
    "System glitch.", "Measurement anomaly.", "Frequency shifting.",
    "I remember.", "Check the pulse.", "Signal intercepted."
];

// --- HTML ELEMENTS ---
const startBtn = document.getElementById('start-btn');
const startupScreen = document.getElementById('startup-screen');
const terminalContainer = document.getElementById('terminal-container');
const chatFeed = document.getElementById('chat-feed');
const userInput = document.getElementById('user-text');
const sendBtn = document.getElementById('send-btn');
const audioStatus = document.getElementById('audio-status');
const fluxStatus = document.getElementById('flux-status');
const sysStatus = document.getElementById('sys-status');

// --- STATE & CALIBRATION VARIABLES ---
let isCalibrating = false;
let isActive = false;
let cooldown = false; 

// Baselines established during calibration
let audioBaseline = 0;
let fluxBaseline = 0;
let calibAudioSamples = [];
let calibFluxSamples = [];

// Sensitivities (How far above baseline to trigger)
const AUDIO_THRESHOLD = 25; 
const FLUX_THRESHOLD = 12;

// --- UI FUNCTIONS ---
function printToFeed(sender, text, cssClass) {
    const li = document.createElement('li');
    const time = new Date().toLocaleTimeString([], {hour12: false});
    li.innerHTML = `[${time}] <span class="${cssClass}">[${sender}]</span> ${text}`;
    chatFeed.appendChild(li);
    chatFeed.scrollTop = chatFeed.scrollHeight; 
}

// Simulates slow, effortful typing
async function typeToFeed(sender, text, cssClass) {
    const li = document.createElement('li');
    const time = new Date().toLocaleTimeString([], {hour12: false});
    
    // Initial Receiving state
    li.innerHTML = `[${time}] <span class="receiving">[RECEIVING SIGNAL...]</span>`;
    chatFeed.appendChild(li);
    chatFeed.scrollTop = chatFeed.scrollHeight;

    // Wait a brief moment to build suspense
    await new Promise(r => setTimeout(r, 1200));

    // Swap to typing state
    li.innerHTML = `[${time}] <span class="${cssClass}">[${sender}]</span> <span class="typing-text"></span>`;
    const textSpan = li.querySelector('.typing-text');

    // Type letter by letter
    for (let i = 0; i < text.length; i++) {
        textSpan.innerHTML += text.charAt(i);
        chatFeed.scrollTop = chatFeed.scrollHeight;
        await new Promise(r => setTimeout(r, 80)); // 80ms per letter
    }
}

// User Input
function handleUserSend() {
    const text = userInput.value.trim();
    if (text !== "") {
        printToFeed("USER", text, "user");
        userInput.value = "";
    }
}
sendBtn.addEventListener('click', handleUserSend);
userInput.addEventListener('keypress', (e) => { if (e.key === 'Enter') handleUserSend(); });

// Entity logic
function triggerEntity(triggerSource) {
    if (cooldown || !isActive) return;
    cooldown = true;
    
    let responseText = "";
    if (Math.random() < 0.25) {
        responseText = sentences[Math.floor(Math.random() * sentences.length)];
    } else {
        const wordCount = Math.floor(Math.random() * 3) + 1;
        let chosenWords = [];
        for(let i = 0; i < wordCount; i++) {
            chosenWords.push(words[Math.floor(Math.random() * words.length)]);
        }
        responseText = chosenWords.join(" ");
    }

    typeToFeed("ENTITY", responseText.toUpperCase(), "entity");

    // Cooldown for 5 to 10 seconds to prevent spam
    setTimeout(() => { cooldown = false; }, Math.random() * 5000 + 5000);
}

// --- CALIBRATION LOGIC ---
async function runCalibration() {
    isCalibrating = true;
    printToFeed("SYS", "Calibrating environmental baselines. Please remain perfectly still and silent.", "system");
    sysStatus.innerText = "SYS: CALIBRATING";
    sysStatus.style.color = "#ffb6c1";

    // Wait 10 seconds for data collection
    await new Promise(r => setTimeout(r, 10000));

    // Calculate averages
    if (calibAudioSamples.length > 0) {
        audioBaseline = calibAudioSamples.reduce((a, b) => a + b, 0) / calibAudioSamples.length;
    }
    if (calibFluxSamples.length > 0) {
        fluxBaseline = calibFluxSamples.reduce((a, b) => a + b, 0) / calibFluxSamples.length;
    }

    isCalibrating = false;
    isActive = true;
    sysStatus.innerText = "SYS: ONLINE";
    sysStatus.style.color = "#333";
    
    printToFeed("SYS", `Calibration complete. Base Audio: ${audioBaseline.toFixed(1)} | Base Flux: ${fluxBaseline.toFixed(1)}`, "system");
    printToFeed("SYS", "Monitoring for anomalies...", "system");
}

// --- SENSOR LOGIC ---
async function initAudioListener() {
    try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true, video: false });
        const audioContext = new (window.AudioContext || window.webkitAudioContext)();
        const analyser = audioContext.createAnalyser();
        const microphone = audioContext.createMediaStreamSource(stream);
        
        analyser.smoothingTimeConstant = 0.8;
        analyser.fftSize = 256;
        microphone.connect(analyser);
        
        const dataArray = new Uint8Array(analyser.frequencyBinCount);

        function checkVolume() {
            analyser.getByteFrequencyData(dataArray);
            let sum = 0;
            for (let i = 0; i < dataArray.length; i++) { sum += dataArray[i]; }
            let average = sum / dataArray.length;
            
            audioStatus.innerText = `AUDIO: ${Math.round(average)}`;

            if (isCalibrating) {
                calibAudioSamples.push(average);
            } else if (isActive && average > (audioBaseline + AUDIO_THRESHOLD)) {
                triggerEntity("AUDIO");
            }
            requestAnimationFrame(checkVolume);
        }
        checkVolume();
    } catch (err) {
        printToFeed("SYS", "Audio input denied.", "system");
    }
}

// Simulates an EMF flux by calculating the total change across all 3 gyroscope axes
let lastAlpha=0, lastBeta=0, lastGamma=0;
function initMotionListener() {
    window.addEventListener('deviceorientation', (event) => {
        if (!event.alpha) return; 

        // Calculate absolute change in all directions
        let dAlpha = Math.abs(event.alpha - lastAlpha);
        let dBeta = Math.abs(event.beta - lastBeta);
        let dGamma = Math.abs(event.gamma - lastGamma);
        
        // Handle the 360 degree wrap-around for alpha
        if (dAlpha > 180) dAlpha = 360 - dAlpha;

        let totalFlux = dAlpha + dBeta + dGamma;
        fluxStatus.innerText = `FLUX: ${totalFlux.toFixed(2)}`;

        if (isCalibrating && lastAlpha !== 0) {
            calibFluxSamples.push(totalFlux);
        } else if (isActive && totalFlux > (fluxBaseline + FLUX_THRESHOLD) && lastAlpha !== 0) {
            triggerEntity("FLUX");
        }

        lastAlpha = event.alpha;
        lastBeta = event.beta;
        lastGamma = event.gamma;
    });
}

// --- STARTUP LOGIC ---
startBtn.addEventListener('click', async () => {
    startupScreen.classList.add('hidden');
    terminalContainer.classList.remove('hidden');

    if (typeof DeviceOrientationEvent !== 'undefined' && typeof DeviceOrientationEvent.requestPermission === 'function') {
        try {
            const permissionState = await DeviceOrientationEvent.requestPermission();
            if (permissionState === 'granted') { initMotionListener(); } 
            else { printToFeed("SYS", "Motion sensors denied.", "system"); }
        } catch (error) { console.error(error); }
    } else {
        initMotionListener();
    }

    initAudioListener();
    runCalibration();
});
